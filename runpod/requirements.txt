# RunPod MeloTTS Requirements
# Core dependencies for serverless deployment

# RunPod SDK
runpod>=1.6.0

# Audio processing
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.21.0

# Additional dependencies that MeloTTS might need
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
scipy>=1.9.0
pydub>=0.25.1

# Comprehensive dependency conflict fixes
botocore>=1.31.0,<1.33.0
boto3>=1.28.0,<1.30.0
# Fix HTTPClientError attribute issues
urllib3>=1.26.0,<2.0.0
# Pin working versions of problematic packages
cached-path>=1.3.0,<1.4.0
# Ensure compatible requests version
requests>=2.28.0,<3.0.0
# Pin compatible s3transfer
s3transfer>=0.6.0,<0.7.0
# Pin dateutil to avoid conflicts
python-dateutil>=2.8.0,<3.0.0

# Optional: for better performance
numba>=0.56.0